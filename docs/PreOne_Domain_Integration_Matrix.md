# PreOne — Domain Integration Matrix (M01)

**References:** PreOne DDD (bounded contexts) · PRD (module map) · BRC (business rules) · ADRs (tenancy/RBAC/audit/API) · ERD/Prisma Schema v3.0 · API Contract Catalog
**Reads the matrix as:** each row = a bounded context; what it reads/writes, the events it produces/consumes, and the scope invariants it preserves. "Read" = published API/domain-service access; private table coupling is forbidden (Spec §47).

| Context | Reads From | Writes To | Events Produced | Events Consumed | APIs (v1) | Business Rules | Security | AY Scope | Branch Scope | Tenant Scope |
|---|---|---|---|---|---|---|---|---|---|---|
| **M00 Setup** | Branch, AcademicSession, Classroom, Program, Facility, StaffProfile, FeePlan, User, SchoolConfig | SchoolSetup, SchoolSetupStep, SchoolSetupValidationRun, SchoolConfig | SchoolSetupCompleted, AcademicYearCreated, ProgramConfigured, SectionCreated, TeacherAssigned | — | /setup/{status,progress,dependencies,validate,go-live}, /setup/steps/[key], /setup/config/[domain], /setup/school-profile, /setup/import/students | Setup state machine (NOT_STARTED→…→LIVE); dependency graph; drift detection | settings:read/write; platform:manage gated to PLATFORM_ADMIN | Current session from wizard | Branch entity managed here | tenantId on every row |
| **Academics (AY/Program/Class/Section)** | — (master data owner) | AcademicSession, Program, Classroom, StudentAllocation | SectionCreated, StudentAllocated, StudentPromoted, AcademicYearClosed | — | /academic-years(+id, close, promote), /programs, /classrooms(+id), /students/[id]/allocate | AY status machine; capacity guard on allocation; allocation history immutable | students:write (allocate), settings:write (AY) | **Owner of AY scoping** — Classroom carries academicSessionId | classroom.branchId | tenantId |
| **Admissions** | Lead, Program capacity, ADMISSION config, ApplicationDocument | AdmissionApplication, Lead, ApplicationDocument | ApplicationSubmitted, ApplicationApproved, StudentCreated | — | /leads(+convert), /applications(+verify/approve/waitlist) | Required-docs gate (config-driven); one admission → one student; single-tx fan-out | admissions:read/write/approve | via target classroom's session | app.branchId | tenantId |
| **Student** | Classroom (live pointer) | Student, StudentAllocation, Guardian, StudentGuardian | StudentAllocated | StudentCreated | /students(+id), /students/[id]/allocate | allocation is mutable per AY, history never overwritten | students:read/write; parent only via guardian links | allocation rows carry academicSessionId | student.branchId | tenantId |
| **Attendance / Daily Ops** | OPERATING config (timings, working days, late rule), CalendarEvent, Classroom roster, DAILY_OPERATIONS config | Attendance, TimelineEntry | AttendanceExceptionDetected | AttendanceExceptionDetected (follow-up+notify) | /attendance, /care, /operations/pickup | Working-day/holiday guard; LATE after arrival window; exception per absent child (deduped); record types config-driven; pickup BLOCK rules | attendance:read/mark; timeline:read for parents | attendance.academicSessionId | classroom.branchId | tenantId |
| **Health/Safety** | HEALTH_SAFETY config (categories, escalation) | TimelineEntry (HEALTH_CHECK/INCIDENT), FollowUp | HealthIncidentCreated, PickupBlocked | HealthIncidentCreated → follow-up+alert | /care (HEALTH_CHECK/INCIDENT), /operations/pickup | Never a plain notification; EMERGENCY → PRINCIPAL ownership; unauthorized pickup always BLOCK+alert | attendance:mark; severity-gated visibility | via classroom session | branch of classroom | tenantId |
| **Communication** | COMMUNICATION config (events/channels), Students/Guardians | Announcement, TimelineEntry | — (funnel, not producer) | All notification events | (lib/notify.ts funnel; no direct module sends) | Event-gated delivery; care records always written; child events → TimelineEntry, broadcasts → Announcement | communication:read/broadcast | carried on TimelineEntry | audience scoping | tenantId |
| **Finance** | FeePlan, FINANCE config (due offset), Student | Invoice, InvoiceItem, Payment, Receipt | InvoiceIssued, InvoiceOverdue, PaymentReceived | InvoiceOverdue → follow-up; PaymentReceived → auto-resolve | /fee-plans, /invoices(+id), /invoices/[id]/payments, /invoices/[id]/remind | Money in integer paise; issued invoice immutable (payments only); overdue sync on read; reminder ≠ resolution | finance:read/write | invoice.academicSessionId | invoice.branchId | tenantId |
| **HR/Staff** | User, TenantUser | StaffProfile | TeacherAssigned | — | /staff, /users, /classrooms PATCH (assignment) | creation ≠ assignment; substitution = assignment change (audited) | users:read/write | assignment via classroom session | staffProfiles.branchId | tenantId |
| **Administration** | Branch, Facility | Facility, Branch.capacity | — | — | /branches, /facilities | Branch is admin boundary; setup-created facilities visible | settings:read/write | n/a | **Owner of branch scoping** | tenantId |
| **Reports / Command Centre** | Cross-context read models (attendance, follow-ups, invoices, timeline, announcements) | — (read-only) | — | — | /operations/today, /operations/exceptions, /dashboard, /teacher/today, /parent/today | Exception-driven presentation (CRITICAL/ATTENTION/NORMAL); default scope = current branch + current AY + today | operations:read/write; timeline:read (parent) | current AY default | current branch default | tenantId |
| **Transport / Inventory** | — | — | — | — | — | **NOT IMPLEMENTED in MVP** — no bounded context exists; feature exposure stays OFF via configuration (M01 Impact Map I-15) | n/a | n/a | n/a | n/a |
| **Parent Portal** | Own children (guardian links), published observations, care timeline, invoices, alerts | — (read-only + payments UI) | — | — | /parent/today, /timeline, /finance, /communication | Child-centric UX (MY CHILD → TODAY/CARE/LEARNING/HEALTH/FEES); internal notes/FollowUp detail never leak; only linked children | timeline:read, communication:read, finance:read | current via children's classrooms | via children's branch | tenantId |
| **Teacher Portal** | Assigned sections (primaryTeacherId), rosters, own follow-ups | Attendance, TimelineEntry (care), Observation, FollowUp transitions | ObservationRecorded | AttendanceExceptionDetected | /teacher/today, /attendance, /care, /observations, /operations/follow-ups | Sees actions, not config tables; only assigned sections; no manual class lists | attendance:mark, academics:write, operations:read/write | via classroom session | via classroom branch | tenantId |
| **Follow-up Engine (cross-context)** | All contexts via domain events | FollowUp | FollowUpCreated/Resolved | All domain exceptions | /operations/follow-ups(+id) | Notification ≠ resolution; dedupe per source; full state machine + audit; unresolved stays visible | operations:read/write | carried when source has AY | carried when source has branch | tenantId |
