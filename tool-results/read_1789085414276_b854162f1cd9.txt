     1→   418→            )}
     2→   419→
     3→   420→            {kind === 'programs' && (
     4→   421→              <>
     5→   422→                <TableToolbar title="Programs" count={list.length} onAdd={() => setModal('program')} addLabel="Add Program" />
     6→   423→                {list.length === 0 ? <EmptyState icon={<Blocks size={40} />} title="No programs configured" message="Define the programs your preschool runs — Playgroup, Nursery, Jr KG, Sr KG, Daycare or fully custom programs." /> : (
     7→   424→                  <div className="dtable-scroll"><table className="dtable">
     8→   425→                    <thead><tr><th>Program</th><th>Age band</th><th>Capacity</th><th>Classes</th><th>Fee plan</th><th>Status</th></tr></thead>
     9→   426→                    <tbody>{list.map((p: Dict) => (
    10→   427→                      <tr key={String(p.id)}>
    11→   428→                        <td><span className="cell-strong">{p.name}</span><span className="cell-sub">{p.code} · {String(p.programType).toLowerCase()}</span></td>
    12→   429→                        <td>{p.ageMinMonths ?? '—'}–{p.ageMaxMonths ?? '—'} mo</td>
    13→   430→                        <td>{String(p.capacity)}</td>
    14→   431→                        <td>{String(p.classrooms)}</td>
    15→   432→                        <td>{p.hasFeePlan ? <span className="badge b-success">linked</span> : <span className="badge b-warning">none</span>}</td>
    16→   433→                        <td><StatusBadge status={p.isActive ? 'ACTIVE' : 'INACTIVE'} /></td>
    17→   434→                      </tr>
    18→   435→                    ))}</tbody>
    19→   436→                  </table></div>
    20→   437→                )}
    21→   438→              </>
    22→   439→            )}
    23→   440→
    24→   441→            {kind === 'roles' && (
    25→   442→              <>
    26→   443→                <p className="card-sub" style={{ marginBottom: 14 }}>PreOne uses one RBAC system. Members below already carry platform-enforced roles — invite more staff in Settings → Staff.</p>
    27→   444→                <div className="dtable-scroll"><table className="dtable">
    28→   445→                  <thead><tr><th>Member</th><th>Role</th><th>Status</th></tr></thead>
    29→   446→                  <tbody>{list.map((u: Dict) => (
    30→   447→                    <tr key={String(u.id)}>
    31→   448→                      <td><span className="cell-strong">{u.name}</span><span className="cell-sub">{u.email}</span></td>
    32→   449→                      <td><span className="badge b-primary">{String(u.role).replaceAll('_', ' ')}</span></td>
    33→   450→                      <td><StatusBadge status={String(u.status)} /></td>
    34→   451→                    </tr>
    35→   452→                  ))}</tbody>
    36→   453→                </table></div>
    37→   454→                <p className="helper" style={{ marginTop: 10 }}>This step completes automatically once the owner plus at least one operator account exist.</p>
    38→   455→              </>
    39→   456→            )}
    40→   457→
    41→   458→            {kind === 'years' && (
    42→   459→              <>
    43→   460→                <TableToolbar title="Academic years" count={list.length} onAdd={() => setModal('year')} addLabel="Create Academic Year" />
    44→   461→                {list.length === 0 ? <EmptyState icon={<CalendarPlus size={40} />} title="No academic year exists" message="Create the operating year — enrolment, attendance, fees and reports all hang from it. Historical years stay queryable forever." /> : (
    45→   462→                  <div className="dtable-scroll"><table className="dtable">
    46→   463→                    <thead><tr><th>Year</th><th>Range</th><th>Classes</th><th>Status</th><th></th></tr></thead>
    47→   464→                    <tbody>{list.map((y: Dict) => (
    48→   465→                      <tr key={String(y.id)}>
    49→   466→                        <td><span className="cell-strong">{y.name}</span>{y.isCurrent && <span className="badge b-success" style={{ marginLeft: 6 }}>current</span>}</td>
    50→   467→                        <td>{new Date(String(y.startDate)).toLocaleDateString()} → {new Date(String(y.endDate)).toLocaleDateString()}</td>
    51→   468→                        <td>{String(y.classrooms)}</td>
    52→   469→                        <td><StatusBadge status={String(y.status)} /></td>
    53→   470→                        <td>{!y.isCurrent && <button className="btn btn-sm btn-outline" onClick={async () => { const j = await api(`/api/v1/academic-years/${y.id}`, 'PATCH', { setStatusCurrent: true }); if (j.success) { toast.success('Current year set', String(y.name)); refreshAll() } else toast.error('Failed', j.error?.message) }}>Set current</button>}</td>
    54→   471→                      </tr>
    55→   472→                    ))}</tbody>
    56→   473→                  </table></div>
    57→   474→                )}
    58→   475→              </>
    59→   476→            )}
    60→   477→
    61→   478→            {kind === 'calendar' && (
    62→   479→              <>
    63→   480→                <TableToolbar title="Calendar events" count={list.length} onAdd={() => setModal('event')} addLabel="Add Event" />
    64→   481→                {list.length === 0 ? <EmptyState icon={<CalendarDays size={40} />} title="No calendar events" message="Add holidays, vacations, parent meetings, assessment periods and special days — attendance understands them." /> : (
    65→   482→                  <div className="dtable-scroll"><table className="dtable">
    66→   483→                    <thead><tr><th>Date</th><th>Type</th><th>Title</th><th></th></tr></thead>
    67→   484→                    <tbody>{list.map((e: Dict) => (
    68→   485→                      <tr key={String(e.id)}>
    69→   486→                        <td>{new Date(String(e.date)).toLocaleDateString()}</td>
    70→   487→                        <td><span className="badge b-info">{String(e.type).replaceAll('_', ' ')}</span></td>
    71→   488→                        <td><span className="cell-strong">{e.title}</span><span className="cell-sub">{e.notes ?? ''}</span></td>
    72→   489→                        <td><button className="btn btn-sm btn-ghost" onClick={async () => { const j = await api(`/api/v1/calendar/${e.id}`, 'DELETE'); if (j.success) { toast.info('Removed'); refreshAll() } else toast.error('Failed', j.error?.message) }}><Trash2 size={13} /></button></td>
    73→   490→                      </tr>
    74→   491→                    ))}</tbody>
    75→   492→                  </table></div>
    76→   493→                )}
    77→   494→              </>
    78→   495→            )}
    79→   496→
    80→   497→            {kind === 'infrastructure' && <InfraSection rooms={list} onDone={refreshAll} onAddFacility={() => setModal('facility')} />}
    81→   498→
    82→   499→            {kind === 'classes' && <ClassesSection rooms={list} onDone={refreshAll} />}
    83→   500→
    84→   501→            {kind === 'teachers' && <TeachersSection rooms={list} onDone={refreshAll} />}
    85→   502→
    86→   503→            {kind === 'staff' && <StaffSection staff={list} onDone={refreshAll} onAdd={() => setModal('staff')} />}
    87→   504→
    88→   505→            {kind === 'fees' && (
    89→   506→              <>
    90→   507→                <p className="card-sub" style={{ marginBottom: 14 }}>Every active program needs an active fee plan — invoices can never be generated without valid fee configuration. Fee heads and plan items live in <a href="/app/settings" className="cell-link">Settings → Fees</a> (existing Finance domain).</p>
    91→   508→                <div className="dtable-scroll"><table className="dtable">
    92→   509→                  <thead><tr><th>Program</th><th>Fee plan</th><th>Classes</th></tr></thead>
    93→   510→                  <tbody>{list.map((p: Dict) => (
    94→   511→                    <tr key={String(p.id)}>
    95→   512→                      <td><span className="cell-strong">{p.name}</span><span className="cell-sub">{p.code}</span></td>
    96→   513→                      <td>{p.hasFeePlan ? <span className="badge b-success">active plan linked</span> : <span className="badge b-danger">no fee plan</span>}</td>
    97→   514→                      <td>{String(p.classrooms)}</td>
    98→   515→                    </tr>
    99→   516→                  ))}</tbody>
   100→   517→                </table></div>
   101→   518→                <div style={{ marginTop: 14 }}>
   102→   519→                  <p className="card-sub" style={{ marginBottom: 8 }}>Accepted payment methods (Finance configuration):</p>
   103→   520→                  <QuickFinanceConfig onDone={refreshAll} api={api} toast={toast} />
   104→   521→                </div>
   105→   522→              </>
   106→   523→            )}
   107→   524→
   108→   525→            {kind === 'import' && (
   109→   526→              <>
   110→   527→                <p className="card-sub" style={{ marginBottom: 12 }}>
   111→   528→                  CSV header: <code>firstName,lastName,dob,gender,admissionNo,guardianName,guardianPhone,relationship</code><br />
   112→   529→                  Rows are validated first — duplicates are flagged and never silently created. Preview → Import.
   113→   530→                </p>
   114→   531→                <textarea className="textarea" rows={6} value={csv} onChange={(e) => setCsv(e.target.value)} placeholder={'firstName,lastName,dob,gender,admissionNo,guardianName,guardianPhone,relationship\nAarav,Sharma,2022-04-12,MALE,IMP-001,Priya Sharma,9876543210,MOTHER'} />
   115→   532→                <div style={{ display: 'flex', gap: 10, marginTop: 12, flexWrap: 'wrap' }}>
   116→   533→                  <button className="btn btn-outline" onClick={async () => { const j = await api('/api/v1/setup/import/students', 'POST', { mode: 'preview', csv }); if (j.success) { setImportPreview(j.data); toast.info('Preview ready', j.data.message) } else toast.error('Preview failed', j.error?.message) }} disabled={!csv.trim()}>
   117→   534→                    <Upload size={15} /> Validate & Preview
   118→   535→                  </button>
   119→   536→                  {importPreview && Number(importPreview.valid) > 0 && (
   120→   537→                    <button className="btn btn-primary" onClick={async () => { const j = await api('/api/v1/setup/import/students', 'POST', { mode: 'commit', csv }); if (j.success) { toast.success('Import complete', j.data.message); setImportPreview(null); setCsv(''); loadStatus() } else toast.error('Import failed', j.error?.message) }}>
   121→   538→                      <Upload size={15} /> Import {String(importPreview.valid)} students
   122→   539→                    </button>
   123→   540→                  )}
   124→   541→                </div>
   125→   542→                {importPreview && (
   126→   543→                  <div style={{ marginTop: 12, maxHeight: 260, overflowY: 'auto', border: '1px solid var(--border-subtle)', borderRadius: 10 }}>
   127→   544→                    <table className="dtable"><thead><tr><th>Row</th><th>Child</th><th>Admission No</th><th>Guardian</th><th>Status</th></tr></thead>
   128→   545→                      <tbody>{(importPreview.rows as Dict[]).map((r) => (
   129→   546→                        <tr key={String(r.row)}>
   130→   547→                          <td>{String(r.row)}</td>
   131→   548→                          <td>{r.firstName} {r.lastName}</td>
   132→   549→                          <td>{r.admissionNo}</td>
   133→   550→                          <td>{r.guardian}</td>
   134→   551→                          <td>{r.status === 'READY' ? <span className="badge b-success">READY</span> : <span className="badge b-danger" title={(r.errors as string[])?.join('; ')}>ERROR</span>}</td>
   135→   552→                        </tr>
   136→   553→                      ))}</tbody></table>
   137→   554→                  </div>
   138→   555→                )}
   139→   556→              </>
   140→   557→            )}
   141→   558→
   142→   559→            {kind === 'unknown' && <EmptyState icon={<Settings2 size={40} />} title="Nothing to configure" message="This step completes from your existing data — mark it complete below." />}
   143→   560→
   144→   561→            {/* footer complete for non-form kinds */}
   145→   562→            {kind !== 'config' && kind !== 'branding' && kind !== 'profile' && me?.status !== 'COMPLETE' && me?.status !== 'SKIPPED' && (
   146→   563→              <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 18, paddingTop: 14, borderTop: '1px solid var(--border-subtle)' }}>
   147→   564→                <span className="helper" style={{ marginRight: 'auto' }}>Completion is verified against your real data — this records who completed the step and when.</span>
   148→   565→                <button className="btn btn-primary" onClick={() => completeStep(true)} disabled={saving}><CheckCircle2 size={15} /> Mark Step Complete</button>
   149→   566→              </div>
   150→   567→            )}
   151→   568→          </div>
   152→   569→        </div>
   153→   570→      )}
   154→   571→
   155→   572→      {/* ── MODALS (rendered from parent with real api/toast) ── */}
   156→   573→      {modal === 'branch' && <BranchModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   157→   574→      {modal === 'program' && <ProgramModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   158→   575→      {modal === 'year' && <YearModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   159→   576→      {modal === 'event' && <EventModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   160→   577→      {modal === 'facility' && <FacilityModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   161→   578→      {modal === 'staff' && <StaffModal onClose={() => setModal(null)} onDone={refreshAll} api={api} toast={toast} />}
   162→   579→    </>
   163→   580→  )
   164→   581→}
   165→   582→
   166→   583→/* ────────────────────────── section components ────────────────────────── */
   167→   584→function InfraSection({ rooms, onDone, onAddFacility }: { rooms: Dict[]; onDone: () => void; onAddFacility: () => void }) {
   168→   585→  const [facilities, setFacilities] = useState<Dict[] | null>(null)
   169→   586→  const loadF = useCallback(async () => { const j = await fetch('/api/v1/facilities').then((r) => r.json()); if (j.success) setFacilities(j.data) }, [])
   170→   587→  useEffect(() => { Promise.resolve().then(loadF) }, [loadF])
   171→   588→  return (
   172→   589→    <>
   173→   590→      <TableToolbar title="Rooms (classrooms)" count={rooms.length} icon={<LayoutGrid size={14} style={{ marginRight: 6, verticalAlign: -2 }} />} />
   174→   591→      <p className="helper" style={{ marginBottom: 10 }}>Rooms are managed in <a href="/app/settings" className="cell-link">Settings → Classes</a> — every active room counts toward this step; other areas are registered here.</p>
   175→   592→      <div className="dtable-scroll"><table className="dtable">
   176→   593→        <thead><tr><th>Room</th><th>Program</th><th>Capacity</th><th>Teacher</th><th>Students</th></tr></thead>
   177→   594→        <tbody>{rooms.map((c: Dict) => (
   178→   595→          <tr key={String(c.id)}>
   179→   596→            <td><span className="cell-strong">{c.name}</span><span className="cell-sub">{c.code}</span></td>
   180→   597→            <td>{c.programName ?? String(c.programType).toLowerCase()}</td>
   181→   598→            <td>{String(c.capacity)}</td>
   182→   599→            <td>{c.teacher ?? <span className="badge b-warning">unassigned</span>}</td>
   183→   600→            <td>{String(c.students)}</td>
   184→   601→          </tr>
   185→   602→        ))}</tbody>
   186→   603→      </table></div>
   187→   604→      <div className="table-toolbar" style={{ margin: '16px 0 10px' }}>
   188→   605→        <div className="card-title">Other facilities <span className="badge b-neutral">{facilities?.length ?? '…'}</span></div>
   189→   606→        <button className="btn btn-primary btn-sm" onClick={onAddFacility}><Plus size={14} /> Register Facility</button>
   190→   607→      </div>
   191→   608→      {facilities !== null && facilities.length === 0 ? (
   192→   609→        <EmptyState icon={<DoorOpen size={36} />} title="No play / nap / meal areas yet" message="Recommended: register activity, play, nap, meal areas, washrooms and a medical room for daily operations and safety." />
   193→   610→      ) : (
   194→   611→        <div className="dtable-scroll"><table className="dtable">
   195→   612→          <thead><tr><th>Facility</th><th>Type</th><th>Branch</th><th>Capacity</th></tr></thead>
   196→   613→          <tbody>{(facilities ?? []).map((f: Dict) => (
   197→   614→            <tr key={String(f.id)}>
   198→   615→              <td><span className="cell-strong">{f.name}</span><span className="cell-sub">{f.code}</span></td>
   199→   616→              <td><span className="badge b-info">{String(f.type).replaceAll('_', ' ')}</span></td>
   200→   617→              <td>{f.branchName}</td>
   201→   618→              <td>{f.capacity ?? '—'}</td>
   202→   619→            </tr>
   203→   620→          ))}</tbody>
   204→   621→        </table></div>
   205→   622→      )}
   206→   623→    </>
   207→   624→  )
   208→   625→}
   209→   626→
   210→   627→function ClassesSection({ rooms, onDone }: { rooms: Dict[]; onDone: () => void }) {
   211→   628→  const toast = useToast()
   212→   629→  const [programs, setPrograms] = useState<Dict[]>([])
   213→   630→  useEffect(() => { fetch('/api/v1/programs').then((r) => r.json()).then((j) => { if (j.success) setPrograms(j.data) }) }, [])
   214→   631→  const link = async (id: string, programId: string) => {
   215→   632→    const j = await fetch(`/api/v1/classrooms/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ programId: programId || null }) }).then((r) => r.json())
   216→   633→    if (j.success) { toast.success('Program linked'); onDone() } else toast.error('Failed', j.error?.message)
   217→   634→  }
   218→   635→  return (
   219→   636→    <>
   220→   637→      <TableToolbar title="Class-sections (current year)" count={rooms.length} icon={<LayoutGrid size={14} style={{ marginRight: 6, verticalAlign: -2 }} />} />
   221→   638→      <p className="helper" style={{ marginBottom: 10 }}>New classes are added in <a href="/app/settings" className="cell-link">Settings → Classes</a>. Here you link each class-section to its program — capacity guards prevent over-allocation.</p>
   222→   639→      <div className="dtable-scroll"><table className="dtable">
   223→   640→        <thead><tr><th>Class</th><th>Linked program</th><th>Capacity</th><th>Teacher</th><th>Students</th><th>Link program</th></tr></thead>
   224→   641→        <tbody>{rooms.map((c: Dict) => (
   225→   642→          <tr key={String(c.id)}>
   226→   643→            <td><span className="cell-strong">{c.name}</span><span className="cell-sub">{c.code}</span></td>
   227→   644→            <td>{c.programName ?? <span className="badge b-warning">not linked</span>}</td>
   228→   645→            <td>{String(c.capacity)}</td>
   229→   646→            <td>{c.teacher ?? <span className="badge b-warning">unassigned</span>}</td>
   230→   647→            <td>{String(c.students)}</td>
   231→   648→            <td>
   232→   649→              <select className="select" style={{ maxWidth: 180 }} value={String(c.programId ?? '')} onChange={(e) => link(String(c.id), e.target.value)}>
   233→   650→                <option value="">— choose —</option>
   234→   651→                {programs.map((p) => <option key={String(p.id)} value={String(p.id)}>{p.name}</option>)}
   235→   652→              </select>
   236→   653→            </td>
   237→   654→          </tr>
   238→   655→        ))}</tbody>
   239→   656→      </table></div>
   240→   657→      {rooms.length === 0 && <EmptyState icon={<LayoutGrid size={36} />} title="No classes for the current year" message="Add class-sections in Settings → Classes — each needs a program link and capacity." />}
   241→   658→    </>
   242→   659→  )
   243→   660→}
   244→   661→
   245→   662→function TeachersSection({ rooms, onDone }: { rooms: Dict[]; onDone: () => void }) {
   246→   663→  const toast = useToast()
   247→   664→  const [members, setMembers] = useState<Dict[]>([])
   248→   665→  const [sel, setSel] = useState<Dict | null>(null)
   249→   666→  const [teacherId, setTeacherId] = useState('')
   250→   667→  useEffect(() => { fetch('/api/v1/users').then((r) => r.json()).then((j) => { if (j.success) setMembers(j.data) }) }, [])
   251→   668→  const unassigned = rooms.filter((c) => !c.primaryTeacherId).length
   252→   669→  const assign = async () => {
   253→   670→    if (!sel || !teacherId) return
   254→   671→    const j = await fetch(`/api/v1/classrooms/${sel.id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ primaryTeacherId: teacherId }) }).then((r) => r.json())
   255→   672→    if (j.success) { toast.success('Teacher assigned', sel.name as string); setSel(null); onDone() } else toast.error('Failed', j.error?.message)
   256→   673→  }
   257→   674→  return (
   258→   675→    <>
   259→   676→      <TableToolbar title="Teacher assignment" count={rooms.length} icon={<GraduationCap size={14} style={{ marginRight: 6, verticalAlign: -2 }} />} addLabel={`${unassigned} without teacher`} />
   260→   677→      <div className="dtable-scroll"><table className="dtable">
   261→   678→        <thead><tr><th>Class</th><th>Primary teacher</th><th></th></tr></thead>
   262→   679→        <tbody>{rooms.map((c: Dict) => (
   263→   680→          <tr key={String(c.id)}>
   264→   681→            <td><span className="cell-strong">{c.name}</span><span className="cell-sub">{c.code}</span></td>
   265→   682→            <td>{c.teacher ?? <span className="badge b-warning">unassigned</span>}</td>
   266→   683→            <td><button className="btn btn-sm btn-outline" onClick={() => { setSel(c); setTeacherId('') }}><UserPlus size={13} /> Assign</button></td>
   267→   684→          </tr>
   268→   685→        ))}</tbody>
   269→   686→      </table></div>
   270→   687→      {rooms.length === 0 && <EmptyState icon={<GraduationCap size={36} />} title="No classes to staff yet" message="Classes & Sections must be configured before teachers can be assigned." />}
   271→   688→      {sel && (
   272→   689→        <Modal open onClose={() => setSel(null)} title={`Assign teacher — ${sel.name}`} icon={<GraduationCap size={22} />} iconClass="ic-blue">
   273→   690→          <div className="form-grid">
   274→   691→            <div className="field" style={{ gridColumn: '1 / -1' }}>
   275→   692→              <label>Member <span className="req">*</span></label>
   276→   693→              <select className="select" value={teacherId} onChange={(e) => setTeacherId(e.target.value)}>
   277→   694→                <option value="">Select member…</option>
   278→   695→                {members.map((u) => <option key={String(u.userId)} value={String(u.userId)}>{u.name} ({String(u.role).replaceAll('_', ' ')})</option>)}
   279→   696→              </select>
   280→   697→            </div>
   281→   698→          </div>
   282→   699→          <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   283→   700→            <button className="btn btn-ghost" onClick={() => setSel(null)}>Cancel</button>
   284→   701→            <button className="btn btn-primary" onClick={assign} disabled={!teacherId}>Assign</button>
   285→   702→          </div>
   286→   703→        </Modal>
   287→   704→      )}
   288→   705→    </>
   289→   706→  )
   290→   707→}
   291→   708→
   292→   709→function StaffSection({ staff, onDone, onAdd }: { staff: Dict[]; onDone: () => void; onAdd: () => void }) {
   293→   710→  const toast = useToast()
   294→   711→  const [branches, setBranches] = useState<Dict[]>([])
   295→   712→  useEffect(() => { fetch('/api/v1/branches').then((r) => r.json()).then((j) => { if (j.success) setBranches(j.data) }) }, [])
   296→   713→  const assign = async (id: string, branchId: string) => {
   297→   714→    const j = await fetch('/api/v1/staff', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id, branchId: branchId || null }) }).then((r) => r.json())
   298→   715→    if (j.success) { toast.success(branchId ? 'Branch assigned' : 'Assignment cleared'); onDone() } else toast.error('Failed', j.error?.message)
   299→   716→  }
   300→   717→  return (
   301→   718→    <>
   302→   719→      <TableToolbar title="Staff foundation" count={staff.length} icon={<Users size={14} style={{ marginRight: 6, verticalAlign: -2 }} />} onAdd={onAdd} addLabel="Add Staff" />
   303→   720→      {staff.length === 0 ? <EmptyState icon={<Users size={40} />} title="No staff yet" message="Create staff employment profiles — identity comes from their PreOne account, employment data lives here." /> : (
   304→   721→        <div className="dtable-scroll"><table className="dtable">
   305→   722→          <thead><tr><th>Staff</th><th>Role</th><th>Branch</th><th>Employment</th><th>Classes</th><th>Assign branch</th></tr></thead>
   306→   723→          <tbody>{staff.map((s: Dict) => (
   307→   724→            <tr key={String(s.id)}>
   308→   725→              <td><span className="cell-strong">{s.name}</span><span className="cell-sub">{s.employeeCode} · {s.email}</span></td>
   309→   726→              <td><span className="badge b-primary">{String(s.role ?? '—').replaceAll('_', ' ')}</span></td>
   310→   727→              <td>{s.branchName ?? <span className="badge b-warning">unassigned</span>}</td>
   311→   728→              <td><span className="cell-sub">{String(s.employmentType).toLowerCase()} · joined {s.joiningDate ? new Date(String(s.joiningDate)).toLocaleDateString() : '—'}</span></td>
   312→   729→              <td>{String(s.classesAssigned)}</td>
   313→   730→              <td>
   314→   731→                <select className="select" style={{ maxWidth: 160 }} value={String(s.branchId ?? '')} onChange={(e) => assign(String(s.id), e.target.value)}>
   315→   732→                  <option value="">— none —</option>
   316→   733→                  {branches.map((b) => <option key={String(b.id)} value={String(b.id)}>{b.name}</option>)}
   317→   734→                </select>
   318→   735→              </td>
   319→   736→            </tr>
   320→   737→          ))}</tbody>
   321→   738→        </table></div>
   322→   739→      )}
   323→   740→    </>
   324→   741→  )
   325→   742→}
   326→   743→
   327→   744→function QuickFinanceConfig({ onDone, api, toast }: { onDone: () => void; api: ApiFn; toast: ToastFn }) {
   328→   745→  const [methods, setMethods] = useState<string[]>([])
   329→   746→  const [loaded, setLoaded] = useState(false)
   330→   747→  useEffect(() => {
   331→   748→    fetch('/api/v1/setup/config/FINANCE').then((r) => r.json()).then((j) => {
   332→   749→      if (j.success) setMethods((j.data.data?.paymentMethods as string[]) ?? ['CASH', 'UPI', 'BANK_TRANSFER'])
   333→   750→      setLoaded(true)
   334→   751→    })
   335→   752→  }, [])
   336→   753→  if (!loaded) return <Skeleton h={40} />
   337→   754→  return (
   338→   755→    <div style={{ display: 'flex', gap: 6, flexWrap: 'wrap', alignItems: 'center' }}>
   339→   756→      <IndianRupee size={14} style={{ color: 'var(--foreground-secondary)' }} />
   340→   757→      {['CASH', 'CHEQUE', 'CARD', 'UPI', 'NET_BANKING', 'BANK_TRANSFER'].map((m) => {
   341→   758→        const on = methods.includes(m)
   342→   759→        return <button key={m} className={`badge ${on ? 'b-primary' : 'b-neutral'}`} style={{ cursor: 'pointer', padding: '5px 10px' }} onClick={async () => {
   343→   760→          const next = on ? methods.filter((x) => x !== m) : [...methods, m]
   344→   761→          setMethods(next)
   345→   762→          const j = await api('/api/v1/setup/config/FINANCE', 'PUT', { paymentMethods: next })
   346→   763→          if (j.success) { toast.success('Payment methods updated'); onDone() } else toast.error('Failed', j.error?.message)
   347→   764→        }}>{on ? '✓ ' : ''}{m.replaceAll('_', ' ').toLowerCase()}</button>
   348→   765→      })}
   349→   766→    </div>
   350→   767→  )
   351→   768→}
   352→   769→
   353→   770→/* ────────────────────────── modals ────────────────────────── */
   354→   771→function BranchModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   355→   772→  const [busy, setBusy] = useState(false)
   356→   773→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   357→   774→    e.preventDefault(); setBusy(true)
   358→   775→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   359→   776→    const j = await api('/api/v1/branches', 'POST', fd)
   360→   777→    setBusy(false)
   361→   778→    if (j.success) { toast.success('Branch created', String(j.data.name)); onDone() } else toast.error('Failed', j.error?.message)
   362→   779→  }
   363→   780→  return (
   364→   781→    <Modal open onClose={onClose} title="Add Branch / Campus" icon={<Building2 size={22} />} iconClass="ic-blue">
   365→   782→      <form onSubmit={submit}>
   366→   783→        <div className="form-grid">
   367→   784→          <div className="field"><label>Name <span className="req">*</span></label><input className="input" name="name" required /></div>
   368→   785→          <div className="field"><label>Code <span className="req">*</span></label><input className="input" name="code" required placeholder="MAIN2" /></div>
   369→   786→          <div className="field"><label>City</label><input className="input" name="city" /></div>
   370→   787→          <div className="field"><label>Phone</label><input className="input" name="phone" /></div>
   371→   788→          <div className="field"><label>Opens at</label><input className="input" name="timingOpen" type="time" defaultValue="08:30" /></div>
   372→   789→          <div className="field"><label>Closes at</label><input className="input" name="timingClose" type="time" defaultValue="16:00" /></div>
   373→   790→          <div className="field" style={{ gridColumn: '1 / -1' }}><label>Address</label><input className="input" name="address" /></div>
   374→   791→          <div className="field"><label>Capacity (seats)</label><input className="input" name="capacity" type="number" min="1" /></div>
   375→   792→        </div>
   376→   793→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   377→   794→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   378→   795→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Create Branch</button>
   379→   796→        </div>
   380→   797→      </form>
   381→   798→    </Modal>
   382→   799→  )
   383→   800→}
   384→   801→
   385→   802→function ProgramModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   386→   803→  const [busy, setBusy] = useState(false)
   387→   804→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   388→   805→    e.preventDefault(); setBusy(true)
   389→   806→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   390→   807→    const j = await api('/api/v1/programs', 'POST', fd)
   391→   808→    setBusy(false)
   392→   809→    if (j.success) { toast.success('Program created', String(j.data.name)); onDone() } else toast.error('Failed', j.error?.message)
   393→   810→  }
   394→   811→  return (
   395→   812→    <Modal open onClose={onClose} title="Add Program" icon={<Blocks size={22} />} iconClass="ic-violet">
   396→   813→      <form onSubmit={submit}>
   397→   814→        <div className="form-grid">
   398→   815→          <div className="field"><label>Name <span className="req">*</span></label><input className="input" name="name" required placeholder="Jr KG" /></div>
   399→   816→          <div className="field"><label>Code <span className="req">*</span></label><input className="input" name="code" required placeholder="JKG" /></div>
   400→   817→          <div className="field"><label>System program type <span className="req">*</span></label>
   401→   818→            <select className="select" name="programType" required>
   402→   819→              {['PLAYGROUP', 'NURSERY', 'LKG', 'UKG', 'DAYCARE'].map((t) => <option key={t} value={t}>{t}</option>)}
   403→   820→            </select>
   404→   821→            <div className="helper">Links the program to fee plans and existing integrations — name and code stay fully custom.</div>
   405→   822→          </div>
   406→   823→          <div className="field"><label>Capacity <span className="req">*</span></label><input className="input" name="capacity" type="number" defaultValue={20} min="1" required /></div>
   407→   824→          <div className="field"><label>Min age (months)</label><input className="input" name="ageMinMonths" type="number" /></div>
   408→   825→          <div className="field"><label>Max age (months)</label><input className="input" name="ageMaxMonths" type="number" /></div>
   409→   826→          <div className="field"><label>Duration (months)</label><input className="input" name="durationMonths" type="number" placeholder="12" /></div>
   410→   827→          <div className="field" style={{ gridColumn: '1 / -1' }}><label>Description</label><input className="input" name="description" /></div>
   411→   828→        </div>
   412→   829→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   413→   830→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   414→   831→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Create Program</button>
   415→   832→        </div>
   416→   833→      </form>
   417→   834→    </Modal>
   418→   835→  )
   419→   836→}
   420→   837→
   421→   838→function YearModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   422→   839→  const [busy, setBusy] = useState(false)
   423→   840→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   424→   841→    e.preventDefault(); setBusy(true)
   425→   842→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   426→   843→    const j = await api('/api/v1/academic-years', 'POST', fd)
   427→   844→    setBusy(false)
   428→   845→    if (j.success) { toast.success('Academic year created', String(j.data.name)); onDone() } else toast.error('Failed', j.error?.message)
   429→   846→  }
   430→   847→  return (
   431→   848→    <Modal open onClose={onClose} title="Create Academic Year" icon={<CalendarPlus size={22} />} iconClass="ic-green">
   432→   849→      <form onSubmit={submit}>
   433→   850→        <div className="form-grid">
   434→   851→          <div className="field"><label>Name <span className="req">*</span></label><input className="input" name="name" required placeholder="2026-27" /></div>
   435→   852→          <div className="field"><label>Start date <span className="req">*</span></label><input className="input" name="startDate" type="date" required /></div>
   436→   853→          <div className="field"><label>End date <span className="req">*</span></label><input className="input" name="endDate" type="date" required /></div>
   437→   854→        </div>
   438→   855→        <p className="helper">The first academic year is automatically marked current. Terms can be added in Operating Configuration.</p>
   439→   856→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   440→   857→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   441→   858→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Create Year</button>
   442→   859→        </div>
   443→   860→      </form>
   444→   861→    </Modal>
   445→   862→  )
   446→   863→}
   447→   864→
   448→   865→function EventModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   449→   866→  const [busy, setBusy] = useState(false)
   450→   867→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   451→   868→    e.preventDefault(); setBusy(true)
   452→   869→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   453→   870→    const j = await api('/api/v1/calendar', 'POST', fd)
   454→   871→    setBusy(false)
   455→   872→    if (j.success) { toast.success('Event added', String(fd.title)); onDone() } else toast.error('Failed', j.error?.message)
   456→   873→  }
   457→   874→  return (
   458→   875→    <Modal open onClose={onClose} title="Add Calendar Event" icon={<CalendarPlus size={22} />} iconClass="ic-cyan">
   459→   876→      <form onSubmit={submit}>
   460→   877→        <div className="form-grid">
   461→   878→          <div className="field"><label>Date <span className="req">*</span></label><input className="input" name="date" type="date" required /></div>
   462→   879→          <div className="field"><label>Type <span className="req">*</span></label>
   463→   880→            <select className="select" name="type" required>{['HOLIDAY', 'VACATION', 'EVENT', 'PARENT_MEETING', 'ASSESSMENT', 'SPECIAL_DAY'].map((t) => <option key={t} value={t}>{t.replaceAll('_', ' ')}</option>)}</select>
   464→   881→          </div>
   465→   882→          <div className="field" style={{ gridColumn: '1 / -1' }}><label>Title <span className="req">*</span></label><input className="input" name="title" required /></div>
   466→   883→          <div className="field" style={{ gridColumn: '1 / -1' }}><label>Notes</label><input className="input" name="notes" /></div>
   467→   884→        </div>
   468→   885→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   469→   886→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   470→   887→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Add Event</button>
   471→   888→        </div>
   472→   889→      </form>
   473→   890→    </Modal>
   474→   891→  )
   475→   892→}
   476→   893→
   477→   894→function FacilityModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   478→   895→  const [busy, setBusy] = useState(false)
   479→   896→  const [branches, setBranches] = useState<Dict[]>([])
   480→   897→  useEffect(() => { fetch('/api/v1/branches').then((r) => r.json()).then((j) => { if (j.success) setBranches(j.data) }) }, [])
   481→   898→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   482→   899→    e.preventDefault(); setBusy(true)
   483→   900→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   484→   901→    const j = await api('/api/v1/facilities', 'POST', fd)
   485→   902→    setBusy(false)
   486→   903→    if (j.success) { toast.success('Facility registered', String(j.data.name)); onDone() } else toast.error('Failed', j.error?.message)
   487→   904→  }
   488→   905→  return (
   489→   906→    <Modal open onClose={onClose} title="Register Facility / Area" icon={<DoorOpen size={22} />} iconClass="ic-orange">
   490→   907→      <form onSubmit={submit}>
   491→   908→        <div className="form-grid">
   492→   909→          <div className="field"><label>Branch <span className="req">*</span></label>
   493→   910→            <select className="select" name="branchId" required>{branches.map((b) => <option key={String(b.id)} value={String(b.id)}>{b.name}</option>)}</select>
   494→   911→          </div>
   495→   912→          <div className="field"><label>Type <span className="req">*</span></label>
   496→   913→            <select className="select" name="type" required>{['CLASSROOM', 'ACTIVITY_AREA', 'PLAY_AREA', 'NAP_AREA', 'MEAL_AREA', 'WASHROOM', 'MEDICAL', 'OTHER'].map((t) => <option key={t} value={t}>{t.replaceAll('_', ' ')}</option>)}</select>
   497→   914→          </div>
   498→   915→          <div className="field"><label>Name <span className="req">*</span></label><input className="input" name="name" required /></div>
   499→   916→          <div className="field"><label>Code <span className="req">*</span></label><input className="input" name="code" required placeholder="PLAY-1" /></div>
   500→   917→          <div className="field"><label>Capacity</label><input className="input" name="capacity" type="number" /></div>
   501→   918→          <div className="field"><label>Floor / area</label><input className="input" name="floorOrArea" placeholder="Ground floor, east wing" /></div>
   502→   919→          <div className="field"><label>Age suitability</label><input className="input" name="ageSuitability" placeholder="2–4 years" /></div>
   503→   920→        </div>
   504→   921→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   505→   922→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   506→   923→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Register Facility</button>
   507→   924→        </div>
   508→   925→      </form>
   509→   926→    </Modal>
   510→   927→  )
   511→   928→}
   512→   929→
   513→   930→function StaffModal({ onClose, onDone, api, toast }: { onClose: () => void; onDone: () => void; api: ApiFn; toast: ToastFn }) {
   514→   931→  const [busy, setBusy] = useState(false)
   515→   932→  const [branches, setBranches] = useState<Dict[]>([])
   516→   933→  useEffect(() => { fetch('/api/v1/branches').then((r) => r.json()).then((j) => { if (j.success) setBranches(j.data) }) }, [])
   517→   934→  const submit = async (e: React.FormEvent<HTMLFormElement>) => {
   518→   935→    e.preventDefault(); setBusy(true)
   519→   936→    const fd = Object.fromEntries(new FormData(e.currentTarget).entries())
   520→   937→    const j = await api('/api/v1/staff', 'POST', { ...fd, mode: 'new' })
   521→   938→    setBusy(false)
   522→   939→    if (j.success) { toast.success('Staff created', String(fd.employeeCode)); onDone() } else toast.error('Failed', j.error?.message)
   523→   940→  }
   524→   941→  return (
   525→   942→    <Modal open onClose={onClose} title="Add Staff Member" icon={<UserPlus size={22} />} iconClass="ic-purple" wide>
   526→   943→      <form onSubmit={submit}>
   527→   944→        <div className="form-grid">
   528→   945→          <div className="field"><label>Full name <span className="req">*</span></label><input className="input" name="fullName" required /></div>
   529→   946→          <div className="field"><label>Email (login) <span className="req">*</span></label><input className="input" name="email" type="email" required /></div>
   530→   947→          <div className="field"><label>Temporary password <span className="req">*</span></label><input className="input" name="password" required minLength={6} /></div>
   531→   948→          <div className="field"><label>Role <span className="req">*</span></label>
   532→   949→            <select className="select" name="role" required>{['PRINCIPAL', 'COORDINATOR', 'TEACHER', 'ACCOUNTS', 'RECEPTION'].map((r) => <option key={r} value={r}>{r}</option>)}</select>
   533→   950→          </div>
   534→   951→          <div className="field"><label>Employee code <span className="req">*</span></label><input className="input" name="employeeCode" required placeholder="EMP-001" /></div>
   535→   952→          <div className="field"><label>Branch (operational assignment)</label>
   536→   953→            <select className="select" name="branchId">
   537→   954→              <option value="">— assign later —</option>
   538→   955→              {branches.map((b) => <option key={String(b.id)} value={String(b.id)}>{b.name}</option>)}
   539→   956→            </select>
   540→   957→            <div className="helper">Created ≠ assigned — branch assignment completes the Staff Foundation step.</div>
   541→   958→          </div>
   542→   959→          <div className="field"><label>Designation</label><input className="input" name="designation" /></div>
   543→   960→          <div className="field"><label>Qualification</label><input className="input" name="qualification" /></div>
   544→   961→          <div className="field"><label>Joining date</label><input className="input" name="joiningDate" type="date" /></div>
   545→   962→          <div className="field"><label>Employment type</label>
   546→   963→            <select className="select" name="employmentType">{['REGULAR', 'PART_TIME', 'CONTRACT', 'INTERN'].map((t) => <option key={t} value={t}>{t.replaceAll('_', ' ')}</option>)}</select>
   547→   964→          </div>
   548→   965→          <div className="field"><label>Emergency contact name</label><input className="input" name="emergencyContactName" /></div>
   549→   966→          <div className="field"><label>Emergency contact phone</label><input className="input" name="emergencyContactPhone" /></div>
   550→   967→        </div>
   551→   968→        <div style={{ display: 'flex', justifyContent: 'flex-end', gap: 10, marginTop: 16 }}>
   552→   969→          <button type="button" className="btn btn-ghost" onClick={onClose}>Cancel</button>
   553→   970→          <button type="submit" className={`btn btn-primary ${busy ? 'is-loading' : ''}`} disabled={busy}>Create Staff</button>
   554→   971→        </div>
   555→   972→      </form>
   556→   973→    </Modal>
   557→   974→  )
   558→   975→}
   559→   976→